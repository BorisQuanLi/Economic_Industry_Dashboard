from setuptools import setup, find_packages

setup(
    name="economic-industry-dashboard",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        'pytest',
        # add other dependencies here
    ],
)
