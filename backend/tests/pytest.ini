[pytest]
testpaths = tests
python_files = test_*.py
addopts = --verbose --cov=. --cov-report=term-missing
markers =
    unit: Unit tests
    integration: Integration tests requiring external dependencies
    # Register custom marks to avoid warnings
    custom_mark: Custom mark for specific tests
pythonpath = .
